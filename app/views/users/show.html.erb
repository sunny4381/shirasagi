<% if notice %>
  <div><%= notice %></div>
<% end %>

<dl>
  <dt>Name</dt>
  <dd><%= @item.name %></dd>

  <dt>Email</dt>
  <dd><%= @item.email %></dd>
</dl>

<%= link_to("Edit", action: :edit) %> | <%= link_to("Back", action: :index) %>
