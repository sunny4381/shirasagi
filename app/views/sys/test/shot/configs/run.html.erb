<%= form_for :item, url: { action: :run }, html: { method: :post, multipart: true } do |f| %>
<%= error_messages_for :item %>

<div class="addon-views">
  <div class="addon-view" id="addon-basic">
    <div class="addon-head"><h2><%= t("ss.confirm.run") %></h2></div>
    <div class="addon-body">
      <%= render file: "_run" %>
    </div>
  </div>
</div>

<footer class="send">
  <% if @crud_buttons %>
    <% @crud_buttons.call f %>
  <% else %>
    <%= f.submit t("ss.buttons.run"), class: 'btn-danger delete' %>
    <%= f.button t("ss.buttons.cancel"), class: 'btn-default', type: :reset, onclick: "location.href='#{url_for(action: :show)}'; return false;" %>
  <% end %>
</footer>
<% end %>
